# Evidence

Store evidence as immutable-ish records/artifacts where practical. The wiki may be rewritten; evidence should remain traceable.

`index.json` is metadata. Large/raw artifacts may live elsewhere in the workspace—store a stable path/identifier and hash when useful rather than duplicating sensitive data.

Never put secrets into the evidence ledger.
