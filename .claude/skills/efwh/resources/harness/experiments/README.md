# Experiments

An experiment should reduce an important uncertainty with controlled blast radius.

Prefer the smallest discriminating experiment over a broad build. Record the pre-test hypothesis and predicted observations before running it. Afterward, distinguish **result** from **interpretation**.

Use `EXPERIMENT_TEMPLATE.md` for any experiment whose result could materially change the project's direction.
